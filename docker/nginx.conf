server {
    listen 80;
    server_name jham.duckdns.org;
    return 301 https://$host$request_uri;
}

server {
    listen 443 ssl;
    server_name jham.duckdns.org;

    ssl_certificate /etc/letsencrypt/live/jham.duckdns.org/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/jham.duckdns.org/privkey.pem;
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers HIGH:!aNULL:!MD5;
    ssl_prefer_server_ciphers on;

    client_max_body_size 20M;

    # API → Loco backend
    location /api/ {
        proxy_pass http://backend:5150;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # Static files → React build output
    location / {
        root /usr/share/nginx/html;
        try_files $uri $uri/ /index.html;
    }
}
